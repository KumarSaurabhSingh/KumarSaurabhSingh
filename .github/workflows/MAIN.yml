# GitHub Action for generating a contribution graph with a snake eating your contributions.

name: Generate Snake

on:
  # Runs every 6 hours
  schedule:
    - cron: "0 */6 * * *"
    
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# The sequence of runs in this workflow:
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      # Generate Snake GIF
      - name: Generate Snake Animation
        uses: Platane/snk@master
        id: snake-gif
        with:
          github_user_name: KumarSaurabhSingh
          outputs: |
            dist/github-contribution-grid-snake.svg
            dist/github-contribution-grid-snake.gif

      # Commit and push the generated files to the "output" branch
      - name: Push to output branch
        uses: JamesIves/github-pages-deploy-action@4.1.8
        with:
          branch: output
          folder: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

---

### **🛠 Fixes Applied**
1. **Removed extra dash (`-`) at the beginning of the file.** That was causing the syntax error.
2. **Fixed incorrect indentation** that might have caused parsing issues.
3. **Fixed incorrect parameters under the `with:` section**  
   - Your previous version used:
     ```yaml
     gif_out_path: dist/github-contribution-grid-snake.gif
     svg_out_path: dist/github-contribution-grid-snake.svg
     ```
     The correct syntax is:
     ```yaml
     outputs: |
       dist/github-contribution-grid-snake.svg
       dist/github-contribution-grid-snake.gif
     ```
4. **Updated GitHub Pages Deployment Step**
   - Your original workflow tried to push to `master`, which might not be the correct target.
   - The fix makes sure it pushes to the **"output" branch**.

---

### **✅ Next Steps**
1. Delete the old workflow from your repo.
2. Create a new workflow in `.github/workflows/MAIN.yml` and copy-paste the **fixed version** above.
3. Commit and push the changes.
4. Run the workflow manually from **GitHub Actions** in your repo.
5. After the workflow runs successfully, open:  
