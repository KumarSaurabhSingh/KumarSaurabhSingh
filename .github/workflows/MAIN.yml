  - This will generate both **SVG** and **GIF** versions in the `/dist/snake` directory.

---

#### **5️⃣ Checking Action Logs**
If your workflow isn't working, go to **GitHub > Your Repository > Actions Tab**, select the workflow run, and check for any errors in the logs.  
Also, ensure the `output` branch is actually created and exists in your repository.

---

### **🚀 Fixed & Improved Workflow:**
Here’s the fixed workflow with all corrections:
```yaml
# GitHub Action for generating a contribution graph with a snake eating your contributions.
name: Generate Snake

on:
  schedule:
    - cron: "0 */6 * * *"  # ✅ Corrected indentation

  workflow_dispatch:

jobs:
  build:
    name: Generate Snake Animation
    runs-on: ubuntu-latest

    steps:
      # Checkout repository
      - name: Checkout Repository
        uses: actions/checkout@v4  # ✅ Updated to the latest version

      # Generate the snake graph
      - name: Generate Snake Animation
        id: snake-gif
        uses: Platane/snk@master
        with:
          github_user_name: KumarSaurabhSingh
          outputs: dist/snake  # ✅ Corrected path

      # Check for changes in the 'dist' directory
      - name: Check for changes
        run: git status

      # Push the changes
      - name: Push changes
        uses: ad-m/github-push-action@master  # ✅ Check if 'master' or 'output' branch
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          branch: output  # ✅ Ensure it matches the branch where the files are pushed
          force: true
